language: minimal
services:
  - docker
script:
  - docker login -u $username -p $password
  - echo $TRAVIS_BRANCH
  - docker build -t riblet .
  - if [ "$TRAVIS_BRANCH" = develop ]; then docker tag riblet jnerney/riblet:latest && docker push jnerney/riblet; fi
